# Example apps are standalone consumers — their installed deps / build output and
# generated lockfiles are never committed.
node_modules/
dist/
.angular/
out-tsc/
package-lock.json
yarn.lock
pnpm-lock.yaml
*.tsbuildinfo
