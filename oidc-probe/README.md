# @tracio/oidc-probe

Throwaway package used once to validate npm trusted publishing (OIDC) for the
`@tracio` scope. It contains no code and must not be used as a dependency.

It is deprecated immediately after the validation run and removed from the
repository.
